import fs from 'node:fs'

const path = 'D:/CodeProject/auto/web/src/components/BugDetailDrawer.vue'
let source = fs.readFileSync(path, 'utf8')

source = source.replace('  if (!detail.value) {\n    return null\n  }', '  if (!props.detail) {\n    return null\n  }')
source = source.replace("const headerTitle = computed(() => detail.value?.title || props.summary?.title || '鏈懡鍚嶇己锟斤拷锟?)", "const headerTitle = computed(() => detail.value?.title || props.summary?.title || '-')")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  emit('update:modelValue', false)\n  router.push({\n    path: `/bugs/${detail.value.id}`,\n    query: { workspace: detail.value.workspaceCode },\n  })", "  if (!detail.value) {\n    return\n  }\n  emit('update:modelValue', false)\n  router.push({\n    path: `/bugs/${detail.value.id}`,\n    query: { workspace: detail.value.workspaceCode },\n  })")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  const url = new URL(router.resolve({\n    path: `/bugs/${props.detail.id}`,\n    query: { workspace: props.detail.workspaceCode },\n  }).href, window.location.origin).toString()", "  if (!detail.value) {\n    return\n  }\n  const url = new URL(router.resolve({\n    path: `/bugs/${detail.value.id}`,\n    query: { workspace: detail.value.workspaceCode },\n  }).href, window.location.origin).toString()")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  const summary = [\n    `缺陷编号：${props.detail.bugNo}`,\n    `缺陷标题：${props.detail.title}`,\n    `状态：${formatBugStatus(detail.value.status)}`,\n    `优先级：${detail.value.priority}`,\n    `严重程度：${formatBugSeverity(props.detail.severity)}`,\n    `负责人：${detail.value.assigneeName || '-'}`,\n  ].join('\\n')", "  if (!detail.value) {\n    return\n  }\n  const summary = [\n    `缺陷编号：${detail.value.bugNo}`,\n    `缺陷标题：${detail.value.title}`,\n    `状态：${formatBugStatus(detail.value.status)}`,\n    `优先级：${detail.value.priority}`,\n    `严重程度：${formatBugSeverity(detail.value.severity)}`,\n    `负责人：${detail.value.assigneeName || '-'}`,\n  ].join('\\n')")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  if (selectedAssigneeId.value === (detail.value.assigneeId ?? null)) {", "  if (!detail.value) {\n    return\n  }\n  if (selectedAssigneeId.value === (detail.value.assigneeId ?? null)) {")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  router.push({\n    path: '/cases/manage',\n    query: { workspace: props.detail.workspaceCode },\n  })", "  if (!detail.value) {\n    return\n  }\n  router.push({\n    path: '/cases/manage',\n    query: { workspace: detail.value.workspaceCode },\n  })")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  router.push({\n    path: '/automation/web',\n    query: { workspace: detail.value.workspaceCode, reportId: String(id) },\n  })", "  if (!detail.value) {\n    return\n  }\n  router.push({\n    path: '/automation/web',\n    query: { workspace: detail.value.workspaceCode, reportId: String(id) },\n  })")
source = source.replace("  if (!props.detail) {\n    return\n  }\n  router.push({\n    path: '/automation/web',\n    query: { workspace: detail.value.workspaceCode, taskId: String(id) },\n  })", "  if (!detail.value) {\n    return\n  }\n  router.push({\n    path: '/automation/web',\n    query: { workspace: detail.value.workspaceCode, taskId: String(id) },\n  })")

fs.writeFileSync(path, source)
